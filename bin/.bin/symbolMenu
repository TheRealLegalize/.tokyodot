#!/bin/bash

CHOICE=$(printf "—\n«»\n…\n" | wofi --dmenu --cache-file=/dev/null --hide-search --hide-scroll --width 220 --height 270 -p "Power Menu")

case $CHOICE in
  "—")
    wl-copy "—"
    ;;
  "«»")
    wl-copy "«»"
    ;;
  "…")
    wl-copy "…"
    ;;
esac
